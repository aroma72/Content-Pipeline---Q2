# Drawing Room

Aroma's L&D content factory: session recordings → learner assets (edit, clips, glossary, assignments). Orchestrator loop: Perceive → Plan → Act → Observe → Reflect → Reentry. **Memory + logs updated at 95% context.** Skill modules in `./skills/`; async agents in `./agents/`; prompts in `./prompts/`. Build simple, measure weekly, specialize only if outcomes improve.
