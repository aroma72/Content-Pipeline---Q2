{
  "name": "content-queen-gates",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Quality-gate layer on top of the Content-Pipeline---Q2 video pipeline (Remotion + ElevenLabs + ffmpeg). Pre-render script lints + post-render artifact judges.",
  "scripts": {
    "gates": "node run-gates.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  }
}
